# @premsight/shared-types

Shared TypeScript types and API contracts used by the PremSight web app (and future TS clients).

## Status

Bootstrap only. Placeholder `HealthResponse` type is present. Football domain types are intentionally deferred.
